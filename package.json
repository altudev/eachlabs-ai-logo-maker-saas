{
  "name": "eachlabs-ai-logo-maker-saas",
  "version": "0.1.0",
  "private": true,
  "packageManager": "bun@1.2.22",
  "workspaces": ["apps/*"],
  "scripts": {
    "dev": "turbo dev --filter=api --filter=web",
    "build": "turbo build",
    "lint": "turbo lint",
    "start": "turbo start",
    "db:generate": "turbo db:generate --filter=api",
    "db:migrate": "turbo db:migrate --filter=api",
    "db:push": "turbo db:push --filter=api",
    "db:studio": "turbo db:studio --filter=api"
  },
  "devDependencies": {
    "turbo": "^2.6.1"
  }
}
